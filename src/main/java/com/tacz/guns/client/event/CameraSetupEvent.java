package com.tacz.guns.client.event;

// Imports limpos
import net.fabricmc.api.EnvType;
import net.fabricmc.api.Environment;

@Environment(EnvType.CLIENT)
public class CameraSetupEvent {

    // Stubs estáticos para métodos chamados externamente
    public static void applyLevelCameraAnimation(Object event) {
        // Lógica removida temporariamente
    }

    public static void applyScopeMagnification(Object event) {
        // Lógica removida temporariamente
    }

    public static void applyGunModelFovModifying(Object event) {
        // Lógica removida temporariamente
    }

    public static void applyCameraRecoil(Object event) {
        // Lógica removida temporariamente
    }

    public static void onComputeMovementFov(Object event) {
        // Lógica removida temporariamente
    }
}