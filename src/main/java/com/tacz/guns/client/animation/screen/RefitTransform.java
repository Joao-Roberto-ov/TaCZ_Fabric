package com.tacz.guns.client.animation.screen;

import net.fabricmc.api.EnvType;
import net.fabricmc.api.Environment;

@Environment(EnvType.CLIENT)
public class RefitTransform {

    // Método stub para ser chamado via Mixin no Fabric
    public static void tickInterpolation(float partialTicks) {
        // Lógica de interpolação aqui
    }

    // Se houver outros métodos estáticos chamados, mantenha-os vazios ou com lógica básica
}